// i18n namespace: auth
// Owner: see docs/BUILD_SPEC.md. Add keys as `'auth.someKey': '…'` in BOTH objects.
// Keys must exist in ar and en with identical key sets.

export const authAr = {
} as const;

export const authEn = {
} as const;
